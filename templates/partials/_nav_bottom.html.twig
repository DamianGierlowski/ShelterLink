<div class="fixed bottom-0 z-50 w-full h-16 shadow-inner-big bg-white  ">
    <div class="flex place-content-center">
        <a class="w-1/3 mt-4 flex-1 items-center" href="{{ path(back_redirect, parameters) }}">
            {% if null  != back_redirect %}
            <img src="{{ asset('images/arrow_back.png') }}" alt="" class="h-9 mx-auto">
            {% endif %}
        </a>
        <a class="w-1/3 mt-4 flex-1 " href="{{ path('app_home') }}">
            <img src="{{ asset('images/home.png') }}" alt="" class="h-9 mx-auto">
        </a>
        <div class="w-1/3 mt-4 flex-1 ">
            {#                    <img src="{{ asset('images/search.png') }}" alt="" class="w-24 mx-auto">#}
        </div>
    </div>
</div>