<div class="container">
    <div class="row">
        <div class="col-md-6 mx-auto">
            {{ form_start(form, {'attr': {'class': 'form'} }) }}

            <div class="form-group row mb-2">
                <label for="{{ form.chip.vars.id }}" class="col-sm-2 col-form-label">{{'animals.chip'|trans}}</label>
                <div class="col-sm-10">
                    {{ form_widget(form.chip, {'attr': {'class': 'form-control'} }) }}
                </div>
            </div>
            <div class="form-group row mb-2">
                <label for="{{ form.name.vars.id }}" class="col-sm-2 col-form-label">{{'animals.name'|trans}}</label>
                <div class="col-sm-10">
                    {{ form_widget(form.name, {'attr': {'class': 'form-control'} }) }}
                </div>
            </div>
            <div class="form-group row mb-2">
                <label for="{{ form.colour.vars.id }}" class="col-sm-2 col-form-label">{{'animals.colour'|trans}}</label>
                <div class="col-sm-10">
                    {{ form_widget(form.colour, {'attr': {'class': 'form-control'} }) }}
                </div>
            </div>

            <div class="form-group row mb-2">
                <label for="{{ form.gender.vars.id }}" class="col-sm-2 col-form-label">{{'animals.gender'|trans}}</label>
                <div class="col-sm-10">
                    {{ form_widget(form.gender, {'attr': {'class': 'form-control'} }) }}
                </div>
            </div>

            <div class="form-group row mb-2">
                <label for="{{ form.genre.vars.id }}" class="col-sm-2 col-form-label">{{'animals.genre'|trans}}</label>
                <div class="col-sm-10">
                    {{ form_widget(form.genre, {'attr': {'class': 'form-control'} }) }}
                </div>
            </div>

            <div class="form-group row mb-2">
                <label for="{{ form.room.vars.id }}" class="col-sm-2 col-form-label">{{'animals.room'|trans}}</label>
                <div class="col-sm-10">
                    {{ form_widget(form.room, {'attr': {'class': 'form-control'} }) }}
                </div>
            </div>


            <div class="form-group row mb-2">
                <label for="{{ form.file.vars.id }}" class="col-sm-2 col-form-label">{{'animals.file'|trans}}</label>
                <div class="col-sm-10">
                    {{ form_widget(form.file, {'attr': {'class': 'form-control'} }) }}
                </div>
            </div>


            <button type="submit" class="btn btn-primary">Wyślij</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
